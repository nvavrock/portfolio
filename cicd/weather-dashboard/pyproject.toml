[project]
name = "weather-dashboard"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
src = ["app"]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["app"]
